<div class="d-grid gap-2 col-6 mx-auto my-5">
  <% if (nextRoute) { %>
  <a href="/<%= nextRoute %>" class="btn btn-primary" type="button">Keyingisi</a>
  <% } else { %>
  <button class="btn btn-primary" type="submit">Keyingisi</button>
  <% } %>
</div>
